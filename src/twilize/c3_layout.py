"""C3 template layout — Template TWB Swap approach.

Uses the EXACT XML from the professional Tableau Dashboard Layout Templates
c.3 template. The XML was generated by Tableau Desktop and is guaranteed valid.

Strategy: Copy the template XML verbatim, then surgically swap text placeholder
zones with real worksheet/filter zones. Never touch coordinates or structure.
"""

from __future__ import annotations

import logging
from copy import deepcopy
from typing import Any, Callable

from lxml import etree

logger = logging.getLogger(__name__)

# ── The exact C3 template XML from Tableau Desktop ──────────────────────
# This XML was extracted from "Tableau Dashboard Layout Templates.twbx"
# dashboard "c.3". Every coordinate, margin, and style is Tableau-native.
_C3_TEMPLATE_XML = """\
<zones>
  <zone h="100000" id="1" param="vert" type-v2="layout-flow" w="100000" x="0" y="0">
    <zone fixed-size="70" h="8750" id="2" is-fixed="true" param="horz" type-v2="layout-flow" w="100000" x="0" y="0">
      <zone forceUpdate="true" h="8750" id="5" type-v2="text" w="98666" x="667" y="0">
        <formatted-text>
          <run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="20">TITLE PLACEHOLDER</run>
        </formatted-text>
        <zone-style>
          <format attr="border-color" value="#000000"/>
          <format attr="border-style" value="none"/>
          <format attr="border-width" value="0"/>
          <format attr="margin" value="4"/>
        </zone-style>
      </zone>
      <zone-style>
        <format attr="border-color" value="#000000"/>
        <format attr="border-style" value="none"/>
        <format attr="border-width" value="0"/>
        <format attr="margin-right" value="8"/>
        <format attr="margin-left" value="8"/>
        <format attr="background-color" value="#ffffff"/>
      </zone-style>
    </zone>
    <zone fixed-size="30" h="3750" id="3" is-fixed="true" param="horz" type-v2="layout-flow" w="100000" x="0" y="8750">
      <zone forceUpdate="true" h="3750" id="8" type-v2="text" w="98666" x="667" y="8750">
        <formatted-text>
          <run bold="true" fontalignment="1" fontcolor="#ffffff" fontsize="12">FILTERS PLACEHOLDER</run>
        </formatted-text>
        <zone-style>
          <format attr="border-color" value="#000000"/>
          <format attr="border-style" value="none"/>
          <format attr="border-width" value="0"/>
          <format attr="margin" value="4"/>
        </zone-style>
      </zone>
      <zone-style>
        <format attr="border-color" value="#000000"/>
        <format attr="border-style" value="none"/>
        <format attr="border-width" value="0"/>
        <format attr="margin-right" value="8"/>
        <format attr="margin-left" value="8"/>
        <format attr="background-color" value="#192f3e"/>
      </zone-style>
    </zone>
    <zone fixed-size="100" h="12500" id="16" is-fixed="true" layout-strategy-id="distribute-evenly" param="horz" type-v2="layout-flow" w="100000" x="0" y="12500">
      <zone forceUpdate="true" h="12500" id="17" type-v2="text" w="20000" x="0" y="12500">
        <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">KPI_SLOT_1</run></formatted-text>
        <zone-style>
          <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
          <format attr="margin" value="0"/><format attr="margin-right" value="4"/><format attr="margin-bottom" value="4"/><format attr="margin-left" value="8"/>
          <format attr="background-color" value="#ffffff"/>
        </zone-style>
      </zone>
      <zone forceUpdate="true" h="12500" id="20" type-v2="text" w="20000" x="20000" y="12500">
        <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">KPI_SLOT_2</run></formatted-text>
        <zone-style>
          <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
          <format attr="margin" value="0"/><format attr="margin-right" value="4"/><format attr="margin-bottom" value="4"/><format attr="margin-left" value="4"/>
          <format attr="background-color" value="#ffffff"/>
        </zone-style>
      </zone>
      <zone forceUpdate="true" h="12500" id="21" type-v2="text" w="20000" x="40000" y="12500">
        <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">KPI_SLOT_3</run></formatted-text>
        <zone-style>
          <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
          <format attr="margin" value="4"/><format attr="margin-top" value="0"/>
          <format attr="background-color" value="#ffffff"/>
        </zone-style>
      </zone>
      <zone forceUpdate="true" h="12500" id="19" type-v2="text" w="20000" x="60000" y="12500">
        <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">KPI_SLOT_4</run></formatted-text>
        <zone-style>
          <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
          <format attr="margin" value="0"/><format attr="margin-right" value="4"/><format attr="margin-bottom" value="4"/><format attr="margin-left" value="4"/>
          <format attr="background-color" value="#ffffff"/>
        </zone-style>
      </zone>
      <zone forceUpdate="true" h="12500" id="18" type-v2="text" w="20000" x="80000" y="12500">
        <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">KPI_SLOT_5</run></formatted-text>
        <zone-style>
          <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
          <format attr="margin" value="0"/><format attr="margin-right" value="8"/><format attr="margin-bottom" value="4"/><format attr="margin-left" value="4"/>
          <format attr="background-color" value="#ffffff"/>
        </zone-style>
      </zone>
      <zone-style>
        <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
        <format attr="margin" value="0"/><format attr="margin-top" value="0"/>
      </zone-style>
    </zone>
    <zone fixed-size="1100" h="75000" id="9" is-fixed="true" layout-strategy-id="distribute-evenly" param="horz" type-v2="layout-flow" w="100000" x="0" y="25000">
      <zone h="75000" id="10" layout-strategy-id="distribute-evenly" param="vert" type-v2="layout-flow" w="50000" x="0" y="25000">
        <zone forceUpdate="true" h="37500" id="12" type-v2="text" w="50000" x="0" y="25000">
          <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">SHEET_SLOT_1</run></formatted-text>
          <zone-style>
            <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
            <format attr="margin" value="4"/><format attr="margin-left" value="8"/>
            <format attr="background-color" value="#ffffff"/>
          </zone-style>
        </zone>
        <zone forceUpdate="true" h="37500" id="13" type-v2="text" w="50000" x="0" y="62500">
          <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">SHEET_SLOT_3</run></formatted-text>
          <zone-style>
            <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
            <format attr="margin" value="4"/><format attr="margin-bottom" value="8"/><format attr="margin-left" value="8"/>
            <format attr="background-color" value="#ffffff"/>
          </zone-style>
        </zone>
      </zone>
      <zone h="75000" id="11" layout-strategy-id="distribute-evenly" param="vert" type-v2="layout-flow" w="50000" x="50000" y="25000">
        <zone forceUpdate="true" h="37500" id="14" type-v2="text" w="50000" x="50000" y="25000">
          <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">SHEET_SLOT_2</run></formatted-text>
          <zone-style>
            <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
            <format attr="margin" value="4"/><format attr="margin-right" value="8"/>
            <format attr="background-color" value="#ffffff"/>
          </zone-style>
        </zone>
        <zone forceUpdate="true" h="37500" id="15" type-v2="text" w="50000" x="50000" y="62500">
          <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">SHEET_SLOT_4</run></formatted-text>
          <zone-style>
            <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
            <format attr="margin" value="4"/><format attr="margin-right" value="8"/><format attr="margin-bottom" value="8"/>
            <format attr="background-color" value="#ffffff"/>
          </zone-style>
        </zone>
      </zone>
    </zone>
  </zone>
</zones>
"""

# ── The C2 template — 4 KPIs, no 5th slot ─────────────────────────────
# Extracted from "Tableau Dashboard Layout Templates.twbx" dashboard "c.2".
# Same professional styling, but only 4 KPI slots (25% width each).
# Better when the dashboard has ≤4 KPI metrics.
_C2_TEMPLATE_XML = """\
<zones>
  <zone h="100000" id="1" param="vert" type-v2="layout-flow" w="100000" x="0" y="0">
    <zone fixed-size="70" h="8750" id="2" is-fixed="true" param="horz" type-v2="layout-flow" w="100000" x="0" y="0">
      <zone forceUpdate="true" h="8750" id="5" type-v2="text" w="98666" x="667" y="0">
        <formatted-text>
          <run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="20">TITLE PLACEHOLDER</run>
        </formatted-text>
        <zone-style>
          <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
          <format attr="margin" value="4"/>
        </zone-style>
      </zone>
      <zone-style>
        <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
        <format attr="margin-right" value="8"/><format attr="margin-left" value="8"/>
        <format attr="background-color" value="#ffffff"/>
      </zone-style>
    </zone>
    <zone fixed-size="30" h="3750" id="3" is-fixed="true" param="horz" type-v2="layout-flow" w="100000" x="0" y="8750">
      <zone forceUpdate="true" h="3750" id="8" type-v2="text" w="98666" x="667" y="8750">
        <formatted-text>
          <run bold="true" fontalignment="1" fontcolor="#ffffff" fontsize="12">FILTERS PLACEHOLDER</run>
        </formatted-text>
        <zone-style>
          <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
          <format attr="margin" value="4"/>
        </zone-style>
      </zone>
      <zone-style>
        <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
        <format attr="margin-right" value="8"/><format attr="margin-left" value="8"/>
        <format attr="background-color" value="#192f3e"/>
      </zone-style>
    </zone>
    <zone fixed-size="100" h="12500" id="16" is-fixed="true" layout-strategy-id="distribute-evenly" param="horz" type-v2="layout-flow" w="100000" x="0" y="12500">
      <zone forceUpdate="true" h="12500" id="17" type-v2="text" w="25000" x="0" y="12500">
        <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">KPI_SLOT_1</run></formatted-text>
        <zone-style>
          <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
          <format attr="margin" value="0"/><format attr="margin-right" value="4"/><format attr="margin-bottom" value="4"/><format attr="margin-left" value="8"/>
          <format attr="background-color" value="#ffffff"/>
        </zone-style>
      </zone>
      <zone forceUpdate="true" h="12500" id="20" type-v2="text" w="25000" x="25000" y="12500">
        <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">KPI_SLOT_2</run></formatted-text>
        <zone-style>
          <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
          <format attr="margin" value="0"/><format attr="margin-right" value="4"/><format attr="margin-bottom" value="4"/><format attr="margin-left" value="4"/>
          <format attr="background-color" value="#ffffff"/>
        </zone-style>
      </zone>
      <zone forceUpdate="true" h="12500" id="19" type-v2="text" w="25000" x="50000" y="12500">
        <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">KPI_SLOT_3</run></formatted-text>
        <zone-style>
          <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
          <format attr="margin" value="0"/><format attr="margin-right" value="4"/><format attr="margin-bottom" value="4"/><format attr="margin-left" value="4"/>
          <format attr="background-color" value="#ffffff"/>
        </zone-style>
      </zone>
      <zone forceUpdate="true" h="12500" id="18" type-v2="text" w="25000" x="75000" y="12500">
        <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">KPI_SLOT_4</run></formatted-text>
        <zone-style>
          <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
          <format attr="margin" value="0"/><format attr="margin-right" value="8"/><format attr="margin-bottom" value="4"/><format attr="margin-left" value="4"/>
          <format attr="background-color" value="#ffffff"/>
        </zone-style>
      </zone>
      <zone-style>
        <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
        <format attr="margin" value="0"/><format attr="margin-top" value="0"/>
      </zone-style>
    </zone>
    <zone fixed-size="1100" h="75000" id="9" is-fixed="true" layout-strategy-id="distribute-evenly" param="horz" type-v2="layout-flow" w="100000" x="0" y="25000">
      <zone h="75000" id="10" layout-strategy-id="distribute-evenly" param="vert" type-v2="layout-flow" w="50000" x="0" y="25000">
        <zone forceUpdate="true" h="37500" id="12" type-v2="text" w="50000" x="0" y="25000">
          <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">SHEET_SLOT_1</run></formatted-text>
          <zone-style>
            <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
            <format attr="margin" value="4"/><format attr="margin-left" value="8"/>
            <format attr="background-color" value="#ffffff"/>
          </zone-style>
        </zone>
        <zone forceUpdate="true" h="37500" id="13" type-v2="text" w="50000" x="0" y="62500">
          <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">SHEET_SLOT_3</run></formatted-text>
          <zone-style>
            <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
            <format attr="margin" value="4"/><format attr="margin-bottom" value="8"/><format attr="margin-left" value="8"/>
            <format attr="background-color" value="#ffffff"/>
          </zone-style>
        </zone>
      </zone>
      <zone h="75000" id="11" layout-strategy-id="distribute-evenly" param="vert" type-v2="layout-flow" w="50000" x="50000" y="25000">
        <zone forceUpdate="true" h="37500" id="14" type-v2="text" w="50000" x="50000" y="25000">
          <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">SHEET_SLOT_2</run></formatted-text>
          <zone-style>
            <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
            <format attr="margin" value="4"/><format attr="margin-right" value="8"/>
            <format attr="background-color" value="#ffffff"/>
          </zone-style>
        </zone>
        <zone forceUpdate="true" h="37500" id="15" type-v2="text" w="50000" x="50000" y="62500">
          <formatted-text><run bold="true" fontalignment="1" fontcolor="#111e29" fontsize="15">SHEET_SLOT_4</run></formatted-text>
          <zone-style>
            <format attr="border-color" value="#000000"/><format attr="border-style" value="none"/><format attr="border-width" value="0"/>
            <format attr="margin" value="4"/><format attr="margin-right" value="8"/><format attr="margin-bottom" value="8"/>
            <format attr="background-color" value="#ffffff"/>
          </zone-style>
        </zone>
      </zone>
    </zone>
  </zone>
</zones>
"""

# Slot names in template order
_KPI_SLOTS = ["KPI_SLOT_1", "KPI_SLOT_2", "KPI_SLOT_3", "KPI_SLOT_4", "KPI_SLOT_5"]
_SHEET_SLOTS = ["SHEET_SLOT_1", "SHEET_SLOT_2", "SHEET_SLOT_3", "SHEET_SLOT_4"]


def _select_template_xml(n_kpis: int, n_charts: int) -> str:
    """Auto-select the best template based on dashboard content.

    - C2 (4 KPI slots): when ≤4 KPIs — gives each KPI 25% width
    - C3 (5 KPI slots): when 5 KPIs — gives each KPI 20% width
    Both templates share the same 2x2 chart grid and styling.
    """
    if n_kpis <= 4:
        return _C2_TEMPLATE_XML
    return _C3_TEMPLATE_XML


def _resolve_filter_param(
    field_name: str, ws_name: str, field_registry: Any, editor: Any,
) -> str:
    """Resolve filter field to Tableau column reference."""
    if editor and ws_name:
        try:
            ws_el = editor._find_worksheet(ws_name)
            if ws_el is not None:
                for f_el in ws_el.findall(".//filter"):
                    col = f_el.get("column", "")
                    if field_name in col:
                        return col
        except (ValueError, AttributeError):
            pass
    if field_registry:
        try:
            ci = field_registry.parse_expression(field_name)
            return field_registry.resolve_full_reference(ci.instance_name)
        except (KeyError, ValueError):
            pass
    return field_name


def _swap_text_to_worksheet(zone: etree._Element, ws_name: str) -> None:
    """Convert a text placeholder zone into a worksheet zone.

    Removes type-v2, formatted-text; adds name + show-title.
    Preserves zone-style and all coordinates/attributes.
    """
    # Remove text-specific attributes and children
    if "type-v2" in zone.attrib:
        del zone.attrib["type-v2"]
    if "forceUpdate" in zone.attrib:
        del zone.attrib["forceUpdate"]
    ft = zone.find("formatted-text")
    if ft is not None:
        zone.remove(ft)

    # Add worksheet attributes
    zone.set("name", ws_name)
    zone.set("show-title", "true")

    # Insert layout-cache BEFORE zone-style for "Entire View" fit
    # This tells Tableau to scale the chart content to fill the zone
    zs = zone.find("zone-style")
    cache = etree.Element("layout-cache")
    cache.set("type-h", "scalable")
    cache.set("type-w", "scalable")
    if zs is not None:
        zone.insert(list(zone).index(zs), cache)
    else:
        zone.append(cache)


def _swap_text_to_filter(
    zone: etree._Element,
    filter_fields: list[dict[str, Any]],
    filter_worksheet: str,
    field_registry: Any,
    editor: Any,
    id_fn: Callable[[], int],
) -> None:
    """Replace the filter placeholder text zone with actual filter zones.

    The parent container already has the right styling (dark navy).
    We replace the single text child with N filter zone children.
    """
    parent = zone.getparent()
    if parent is None:
        return

    # Get the zone-style from the parent (container styling)
    parent_zs = parent.find("zone-style")

    # Remove the text placeholder zone
    parent.remove(zone)

    # Remove the parent's zone-style temporarily (must be re-added LAST)
    if parent_zs is not None:
        parent.remove(parent_zs)

    # Pre-resolve filter fields, skip any that can't be resolved
    valid_filters: list[tuple[str, str]] = []
    for f in filter_fields:
        field_name = f.get("field") or f.get("column", "")
        resolved = _resolve_filter_param(
            field_name, filter_worksheet, field_registry, editor,
        )
        # Only include filters that resolved to a proper datasource reference
        if resolved and "[" in resolved and "." in resolved:
            valid_filters.append((field_name, resolved))
        elif resolved and not resolved.startswith("["):
            # Try bracket-wrapping — Tableau may still accept it
            valid_filters.append((field_name, f"[{resolved}]"))

    if not valid_filters:
        # No valid filters — re-add parent zone-style and bail
        if parent_zs is not None:
            parent.append(parent_zs)
        return

    # Add filter zones
    n = len(valid_filters)
    fw = 98666 // n if n else 98666
    base_y = int(parent.get("y", "8750"))

    for fi, (field_name, resolved) in enumerate(valid_filters):
        fz = etree.SubElement(parent, "zone")
        fz.set("id", str(id_fn()))
        fz.set("type-v2", "filter")
        fz.set("name", filter_worksheet)
        fz.set("mode", "multiplevalues")
        fz.set("show-title", "true")
        fz.set("x", str(667 + fi * fw))
        fz.set("y", str(base_y))
        fz.set("w", str(fw))
        fz.set("h", str(int(parent.get("h", "3750"))))
        fz.set("param", resolved)

        # Filter zone-style
        zs = etree.SubElement(fz, "zone-style")
        for attr, val in [
            ("border-color", "#000000"), ("border-style", "none"),
            ("border-width", "0"), ("margin", "4"),
            ("background-color", "#ffffff"),
        ]:
            fmt = etree.SubElement(zs, "format")
            fmt.set("attr", attr)
            fmt.set("value", val)

    # Re-add parent zone-style LAST
    if parent_zs is not None:
        parent.append(parent_zs)


def build_c3_zones(
    zones_el: etree._Element,
    id_fn: Callable[[], int],
    *,
    title: str = "Dashboard",
    kpi_names: list[str] | None = None,
    chart_names: list[str] | None = None,
    filters: list[dict[str, Any]] | None = None,
    filter_worksheet: str = "",
    field_registry: Any = None,
    editor: Any = None,
) -> None:
    """Build C3 template by copying Tableau-native XML and swapping names.

    This is the Template TWB Swap approach — the gold standard:
    1. Parse the exact C3 template XML (generated by Tableau Desktop)
    2. Swap text placeholder zones → real worksheet zones (name only)
    3. Swap filter placeholder → real filter zones
    4. Set title text
    5. Re-number zone IDs
    Never touch coordinates, margins, or structure.
    """
    kpi_names = kpi_names or []
    chart_names = chart_names or []
    filters = filters or []

    # Auto-select the best template based on content
    template_xml = _select_template_xml(len(kpi_names), len(chart_names))
    template = etree.fromstring(template_xml)

    # Build slot→name mappings
    kpi_map: dict[str, str] = {}
    for i, name in enumerate(kpi_names):
        if i < len(_KPI_SLOTS):
            kpi_map[_KPI_SLOTS[i]] = name

    sheet_map: dict[str, str] = {}
    for i, name in enumerate(chart_names):
        if i < len(_SHEET_SLOTS):
            sheet_map[_SHEET_SLOTS[i]] = name

    # Find all zones and process by slot name
    unused_kpi_slots: list[etree._Element] = []
    unused_sheet_slots: list[etree._Element] = []

    for zone in template.iter("zone"):
        ft = zone.find("formatted-text")
        if ft is None:
            continue
        run = ft.find("run")
        if run is None or run.text is None:
            continue
        slot_text = run.text.strip()

        # Title swap
        if slot_text == "TITLE PLACEHOLDER":
            run.text = title or "Dashboard"
            continue

        # Filter swap
        if slot_text == "FILTERS PLACEHOLDER":
            if filters and filter_worksheet:
                _swap_text_to_filter(
                    zone, filters, filter_worksheet,
                    field_registry, editor, id_fn,
                )
            continue

        # KPI swap
        if slot_text in _KPI_SLOTS:
            if slot_text in kpi_map:
                _swap_text_to_worksheet(zone, kpi_map[slot_text])
            else:
                unused_kpi_slots.append(zone)
            continue

        # Sheet swap
        if slot_text in _SHEET_SLOTS:
            if slot_text in sheet_map:
                _swap_text_to_worksheet(zone, sheet_map[slot_text])
            else:
                unused_sheet_slots.append(zone)
            continue

    # Remove unused KPI slots (if fewer KPIs than 5)
    for z in unused_kpi_slots:
        parent = z.getparent()
        if parent is not None:
            parent.remove(z)

    # Remove unused sheet slots (if fewer charts than 4)
    for z in unused_sheet_slots:
        parent = z.getparent()
        if parent is not None:
            parent.remove(z)

    # If no KPIs at all, remove the KPI container row
    if not kpi_names:
        for zone in template.iter("zone"):
            if zone.get("id") == "16":  # KPI container
                parent = zone.getparent()
                if parent is not None:
                    parent.remove(zone)
                break

    # If no filters, remove the filter container row
    if not filters or not filter_worksheet:
        for zone in template.iter("zone"):
            if zone.get("id") == "3":  # Filter container
                parent = zone.getparent()
                if parent is not None:
                    parent.remove(zone)
                break

    # Re-number all zone IDs to be unique using id_fn
    for zone in template.iter("zone"):
        zone.set("id", str(id_fn()))

    # Copy template content into the target zones element
    root_zone = template.find("zone")
    if root_zone is not None:
        zones_el.append(root_zone)

    print(f"[C3-LAYOUT] Built: title={title}, {len(kpi_names)} KPIs, "
          f"{len(chart_names)} charts, {len(filters)} filters")
    logger.info(
        "Built C3 layout: title=%s, %d KPIs, %d charts, %d filters",
        title, len(kpi_names), len(chart_names), len(filters),
    )
